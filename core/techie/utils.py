# def delete_record(query_set, records: list):
#     """
#         Function receives a list of items.
#         Loops through a QuerySet with an instance variable and checks on each iteration if, capitalize 'instance' of the
#         'instance.expectation_value'
#     """
#     # for capitalized_expectance in list(expectance for expectance in all_techie_instance_expectation):
#     #     if str(capitalized_expectance.expectation_value).capitalize() not in [str(exp).capitalize() for exp in
#     #                                                                           expectations]:
#     #         capitalized_expectance.delete()
#
#


